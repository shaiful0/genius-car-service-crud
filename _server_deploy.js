/**
 * 1. create heroku account
 * 2. verify email
 * 3. install heroku cli
 * 4. ----------------
 * make sure : git add . git commit . git push
 * git push heroku main
 * go to heroku dashbord > current project > setting> reveal config vars
 * copy paste config vars from your .env file
 * make sure you have whitelisted all ip address to access mongodb
 * ---------------------------
 * update server
 * --------------
 * make changes
 * make sure : git add . git commit . git push
 *  git push heroku main
 * ------
*/